[package]
name = "yipc"
version = "0.1.0"
edition = "2021"
description = "Pane-to-pane IPC protocol for ymux"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "1"

[dev-dependencies]
