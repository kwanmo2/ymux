[package]
name = "ytheme"
version = "0.1.0"
edition = "2021"
description = "Shared theme and config directory utilities for the ymux tool family"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "5"
